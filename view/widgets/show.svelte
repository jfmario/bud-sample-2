<script>
  export let widget = {}
</script>

<h1>Show Widget!!</h1>

<table border="1" cellpadding="10">
  <thead>
    {#each Object.keys(widget) as key}
      <th>{key}</th>
    {/each}
  </thead>
  <tr>
    {#each Object.keys(widget) as key}
      <td>{widget[key]}</td>
    {/each}
  </tr>
</table>

<br />

<a href={`/widgets`}>Back</a>
<span> | </span>
<a href={`/widgets/${widget.id || 0}/edit`}>Edit</a>

<style>
  table {
    border-collapse: collapse;
  }
</style>
